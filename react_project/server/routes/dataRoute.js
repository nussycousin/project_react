const express = require('express');
const router = express.Router();
const mongoose = require('mongoose')
const userModle = require('../models/userModle.js');
const { route } = require('./userRoute');

router.post("/",async(req,res)=>{
    try {
        // const 
    } catch (error) {
        
    }
})